================================================================================
                       DOCUMENTATION & TECHNICAL SPECIFICATION
                                   HYPERCORE MODULE
================================================================================
 Module Name    : HyperCore
 Version        : v6.8.5 (Build 6850)
 Architecture   : Universal Dual-Kernel GKI (Linux Kernel 5.10 - 6.12+)
 Target Hardware: MediaTek MT6789 Family (Helio G99 / G100 / G200 with Mali GPU)
 Author         : @itswill00 (aerichandesu@noticesa)
 Inspiration    : encore @Rem01Gaming
 Release ZIP    : HyperCore-v6.8.5-b6850-Unified.zip
================================================================================

TABLE OF CONTENTS
--------------------------------------------------------------------------------
1. MODULE OVERVIEW & CORE DESIGN PRINCIPLES
2. REPOSITORY & DIRECTORY STRUCTURE
3. NATIVE C DAEMON ARCHITECTURE (libhypercore.so)
   3.1. Main Loop & Daemon Lifecycle (src/main.c)
   3.2. Universal Dual-Kernel Sysfs I/O (src/sysfs.c)
   3.3. CPU Cluster, Governor & Uclamp Management (src/cpu.c)
   3.4. Mali GPU & MediaTek GED Driver Optimization (src/gpu.c)
   3.5. Dynamic 4-Tier Thermal Mitigation System (src/thermal.c)
   3.6. Auto Game Detector & Foreground Process Scanner (src/gamelist.c)
   3.7. UNIX Domain Socket IPC & Atomic Telemetry Storage (src/ipc.c)
   3.8. Storage I/O Queue & Memory / VM Optimization (src/io.c & src/memory.c)
   3.9. Hardware Charging & Battery Protection Engine (src/charger.c)
   3.10. SHA-256 Binary Integrity Verification (src/integrity.c & src/sha256.c)
   3.11. System Logging Infrastructure (src/log.c)
4. HYPERMOON IN-GAME PERFORMANCE HUD SUBSYSTEM
   4.1. Dual-Engine Architecture (hypermoon_daemon & HyperMoonOverlay.java)
   4.2. Linux Kernel Inotify FileObserver Synchronization
   4.3. Instant Master Switch & Dynamic Geometry Engine
   4.4. Telemetry Metrics, Presets & Color Themes
5. PERFORMANCE PROFILES SPECIFICATION & DYNAMICS
6. MATERIAL DESIGN 3 WEBUI DASHBOARD (VUE 3 + PINIA SPA)
   6.1. Telemetry Store Resilience & Zero-Flicker Architecture
   6.2. Single-File Artifact Distribution & View Modules
7. INSTALLER LIFECYCLE & SECURITY VALIDATION
   7.1. Module Installation Script (customize.sh)
   7.2. Boot Service Launcher (service.sh)
   7.3. Post-FS-Data Initialization (post-fs-data.sh) & Uninstaller (uninstall.sh)
8. BUILD & DEPLOYMENT AUTOMATION (build.sh)
9. DEVELOPMENT INVARIANTS & SYSTEM RULES

================================================================================
1. MODULE OVERVIEW & CORE DESIGN PRINCIPLES
================================================================================
HyperCore is a kernel-level performance optimization and dynamic power management
module engineered exclusively for Android devices powered by the MediaTek MT6789
Family (Helio G99 / MT6789V/CD, Helio G100, and Helio G200). Operating via an
unprivileged native C daemon (`libhypercore.so`) executing as a background root
service, HyperCore continuously optimizes operating frequencies, CPU uclamp
thresholds, graphics devfreq scaling, charging rates, and memory queues.

CORE ARCHITECTURAL PRINCIPLES:
1. STRICT FREQUENCY UNLOCKING (NO STATIC CLOCK LOCKING):
   HyperCore strictly avoids pinning or locking CPU/GPU frequencies to static
   levels. All frequencies scale dynamically according to workload demands and
   kernel governor heuristics (`sugov_ext` / `schedutil`) to prevent thermal
   runaway, battery exhaustion, and hardware degradation.
2. UNIVERSAL DUAL-KERNEL GKI COMPATIBILITY:
   Full native support across Linux Kernel 5.10, 5.15, 6.1, 6.6, up to 6.12+ GKI
   (Android 12 through Android 16) with transparent fallback between Cgroup v1
   (`/dev/cpuset/`) and Cgroup v2 (`/sys/fs/cgroup/`).
3. DYNAMIC MULTI-SENSOR THERMAL PROTECTION:
   Continuous thermal telemetry collected from CPU, GPU, Battery, and PMIC Charger
   sensors with 4-tier progressive mitigation (Tier 0 to Tier 3) and hysteresis.
4. UNIFIED CONSOLIDATION (ZERO MODULE BLOAT):
   Integrated native HUD subsystem (HyperMoon) and charging protection directly
   within the core module, eliminating redundant background services and
   filesystem clutter.
5. MATERIAL DESIGN 3 AMOLED INTERFACE:
   Dark monochrome WebUI designed with zero-flicker Pinia telemetry state and
   sub-millisecond IPC socket responsiveness.

================================================================================
2. REPOSITORY & DIRECTORY STRUCTURE
================================================================================
HyperCore_Module/
├── customize.sh                 # Root installer script for Magisk/KernelSU/APatch
├── service.sh                   # Boot completion daemon and HUD launcher
├── post-fs-data.sh              # Early boot virtual memory optimization
├── uninstall.sh                 # Complete cleanup and sysfs node restoration
├── module.prop                  # Magisk/KernelSU module metadata
├── system.prop                  # Low-overhead Android system properties
├── gamelist.txt                 # Default package-to-profile mapping file
├── build.sh                     # Unified build, test, and release packaging script
├── NOTICE.md                    # Attribution and inspired architecture credits
├── LICENSE                      # GNU General Public License v3.0
├── banner.jpg                   # Standard module banner asset
├── changelog.md                 # Markdown release changelog
├── checksums.txt                # Reference SHA-256 hashes for module files
├── docs/                        # Architecture and technical documentation
│   └── DOCUMENTATION.txt        # Comprehensive technical specification
├── src/                         # Native C daemon source code
│   ├── main.c                   # Entry point, event loop, and signal handling
│   ├── sysfs.c                  # Universal Dual-Kernel sysfs/procfs I/O driver
│   ├── cpu.c                    # CPU cluster frequencies, governor & uclamp
│   ├── gpu.c                    # Mali GPU devfreq & MediaTek GED manager
│   ├── thermal.c                # 4-tier thermal monitoring and mitigation
│   ├── gamelist.c               # Foreground package scanner and game detector
│   ├── ipc.c                    # UNIX domain socket IPC & atomic JSON exporter
│   ├── io.c                     # Block storage read-ahead and queue tuning
│   ├── memory.c                 # Virtual memory and ZRAM compaction tuning
│   ├── charger.c                # Hardware charging speed and battery protection
│   ├── integrity.c              # Checksum calculation and binary verification
│   ├── sha256.c                 # Self-contained native SHA-256 implementation
│   ├── log.c                    # Rotating filesystem logger
│   ├── hud/                     # HyperMoon HUD native subsystem
│   │   ├── hypermoon_daemon.c   # Lightweight C telemetry collector
│   │   └── HyperMoonOverlay.java# Dalvik/Surface overlay renderer
│   └── include/                 # C headers and data structures
│       ├── common.hpp           # Global structs (hw_nodes, core_state, profiles)
│       └── *.hpp                # Subsystem header declarations
├── system/bin/                  # Deployed root utilities
│   └── hypercore-bugreport      # Automated diagnostics and telemetry bundler
└── webui/                       # Material Design 3 WebUI Dashboard (Vue 3 + Vite)
    ├── src/
    │   ├── App.vue              # Root application shell and view container
    │   ├── views/               # Page views (Home, Charger, Games, Logs, Hud, About)
    │   ├── components/          # MD3 UI components (ActionButtons, AppPicker, Modals)
    │   ├── stores/              # Pinia telemetry stores (hyper.js, hypermoon.js)
    │   └── helpers/shell.js     # Native shell bridge (KernelSU, APatch, execCommand)
    └── vite.config.js           # Singlefile inlining build configuration

================================================================================
3. NATIVE C DAEMON ARCHITECTURE (libhypercore.so)
================================================================================

3.1. Main Loop & Daemon Lifecycle (src/main.c)
- `main()` starts by scanning device hardware nodes via `init_hw_nodes()`.
- Establishes signal handlers (`SIGTERM`, `SIGINT`, `SIGPIPE`) for clean shutdown
  and sysfs permission restoration without leaving zombie processes or orphaned locks.
- Initializes UNIX domain socket IPC on an independent POSIX thread (`init_ipc_socket()`).
- Executes the main iteration loop on a 1-second cadence (`sleep(1)`):
  * Scans real-time battery charging current, voltage, and PMIC state.
  * Evaluates foreground active window via `scan_foreground_game()`.
  * Computes peak temperatures across thermal zones via `update_thermal_mitigation()`.
  * Writes atomic JSON status telemetry to `/data/adb/hypercore/status.json`.
  * Applies profile transitions dynamically when workload context shifts.

3.2. Universal Dual-Kernel Sysfs I/O (src/sysfs.c)
Function `sysfs_write_fallback(const char *const paths[], const char *val)`:
- Accepts an ordered array of alternate sysfs/procfs node paths (e.g. Cgroup v1
  `/dev/cpuset/top-app/cpus` versus Cgroup v2 `/sys/fs/cgroup/top-app/cpuset.cpus`).
- Sequentially tests node availability and write permissions.
- Employs a read-before-write cache to avoid redundant filesystem writes if the
  node already contains the target value.
- Provides thread-safe helper routines: `sysfs_read_int()`, `sysfs_read_str()`,
  and `sysfs_write_str()`.

3.3. CPU Cluster, Governor & Uclamp Management (src/cpu.c)
Function `apply_cpu_profile(profile_t profile)`:
- Handles heterogeneous CPU cluster topology:
  * Policy 0: 6x Cortex-A55 Little Cores (cpu0–cpu5)
  * Policy 6: 2x Cortex-A76 Big Cores (cpu6–cpu7)
- Sets CPU scaling governor to `sugov_ext` (MediaTek custom governor) or `schedutil`.
- Util Clamp (uclamp) Configuration:
  * `cpu.uclamp.min`: Sets baseline task utilization floor to eliminate initial
    frame hesitation when intensive bursts occur.
  * `cpu.uclamp.max`: Clamps maximum CPU utilization ceiling during Sleep mode
    or Tier 3 thermal emergency.
- Governor Rate Limits:
  * Adjusts `up_rate_limit_us` (1000 µs in Gaming) and `down_rate_limit_us`
    allowing instantaneous frequency escalation under graphics loads.

3.4. Mali GPU & MediaTek GED Driver Optimization (src/gpu.c)
Function `apply_gpu_profile(profile_t profile)`:
- Manages ARM Mali-G57 MC2 via devfreq sysfs (`/sys/class/devfreq/13000000.mali`
  or `/sys/class/devfreq/soc:mali`) and MediaTek GED driver nodes.
- Establishes a 30ms devfreq polling interval for rapid load sampling.
- Tunes MediaTek GED parameters according to active workload:
  * `gpu_bottom_freq`: Dynamically raises GPU floor frequency during Gaming.
  * `boost_gpu_enable`: Activates MediaTek hardware GPU boost pipeline.
  * `ged_smart_boost`: Allocates GPU frequency dynamically aligned with target FPS.
  * `gx_game_mode`: Engages dedicated low-latency gaming mode in the driver.

3.5. Dynamic 4-Tier Thermal Mitigation System (src/thermal.c)
A 4-tier progressive thermal mitigation algorithm driven by peak hardware sensor
readings with built-in hysteresis guards:

+--------+-------------------+--------------------------------------------------+
| Tier   | Temperature Range | Mitigation Actions                               |
+--------+-------------------+--------------------------------------------------+
| Tier 0 | < 42°C (Optimal)  | Full Performance Unthrottled                     |
| Tier 1 | 42°C – 45°C       | Mild Mitigation: Modest reduction in uclamp min  |
| Tier 2 | 45°C – 48°C       | Moderate Mitigation: Big core frequency ceiling  |
| Tier 3 | > 48°C (Critical) | Emergency Mitigation: Enforced Sleep state clock |
+--------+-------------------+--------------------------------------------------+

Sensor inputs monitored: CPU (`thermal_zone16`/`thermal_zone0`), Battery (`battery/temp`),
GPU (`thermal_zone30`), and Charger IC (`mtk-master-charger`).

3.6. Auto Game Detector & Foreground Process Scanner (src/gamelist.c)
- Reads configured package rules from `/data/adb/hypercore/gamelist.txt`.
- Evaluates foreground window focus using non-blocking window manager checks.
- If a foreground package matches a gamelist entry, the daemon transitions
  the system profile to `Gaming` or `Gaming MOBA`.
- When the application is dismissed or minimized, the daemon restores `Interactive`.

3.7. UNIX Domain Socket IPC & Atomic Telemetry Storage (src/ipc.c)
- Listens on UNIX domain socket `/data/adb/hypercore/hypercore.sock`.
- Processes commands:
  * `GET_STATUS`          : Returns complete system telemetry in JSON format.
  * `GET_PROFILE`         : Returns current active profile name.
  * `SET_PROFILE:<name>`  : Manual override to Sleep, Interactive, Gaming, or MOBA.
  * `ADD_GAME:<pkg>`      : Adds a game package to the dynamic gamelist.
  * `REMOVE_GAME:<pkg>`   : Removes a game package from gamelist.
- Function `update_status_json_file()` writes telemetry atomically using temporary
  files (`.tmp`) and `rename()`, preventing corrupt reads by the WebUI.

3.8. Storage I/O Queue & Memory / VM Optimization (src/io.c & src/memory.c)
- Storage I/O Optimization:
  Sets `read_ahead_kb=256`, `rq_affinity=2`, and `iostats=0` on UFS/eMMC devices
  to minimize CPU interrupt overhead during file operations.
- Virtual Memory Subsystem:
  Tunes `vm.swappiness=60`, `vm.dirty_ratio=15`, `vm.dirty_background_ratio=5`,
  `vm.vfs_cache_pressure=100`, and `vm.page-cluster=0` for optimal ZRAM utilization.

3.9. Hardware Charging & Battery Protection Engine (src/charger.c)
- Interacts directly with physical charging controllers via kernel sysfs nodes
  (`/sys/class/power_supply/battery/charge_control_limit`).
- Provides 16 discrete hardware charging levels (0 through 15) with dynamic thermal
  override protection if battery temperatures exceed 43°C.
- Night charging protection (`night_charging`) and 80% state-of-charge limits (`protect_80`).

3.10. SHA-256 Binary Integrity Verification (src/integrity.c & src/sha256.c)
- Self-contained implementation of the SHA-256 hashing algorithm.
- Invoked during installation via `--verify-integrity` to compare module file
  hashes against embedded checksums, preventing unauthorized binary tampering.

3.11. System Logging Infrastructure (src/log.c)
- Appends operational records, thermal events, and profile switches to
  `/sdcard/Android/hypercore.log`.
- Features automated log rotation to keep file sizes lightweight.

================================================================================
4. HYPERMOON IN-GAME PERFORMANCE HUD SUBSYSTEM
================================================================================

4.1. Dual-Engine Architecture (hypermoon_daemon & HyperMoonOverlay.java)
HyperMoon is an integrated, low-latency on-screen performance monitor:
- Native C Collector (`hypermoon_daemon`): Gathers sub-millisecond FPS metrics
  from SurfaceFlinger / Mali GPU devfreq, CPU cluster loads, battery wattage,
  and network throughput, writing state to `/data/adb/hypercore/hud/stats.json`.
- Java DEX Overlay (`HyperMoonOverlay.java`): Executed via Android `app_process`,
  rendering hardware-accelerated text and pill overlays directly onto a native
  window overlay without requiring third-party APK installations.

4.2. Linux Kernel Inotify FileObserver Synchronization
- Utilizes Android`s `FileObserver` bound to `/data/adb/hypercore/hud/config.json`.
- When settings or sliders are adjusted in the WebUI, the Linux kernel triggers
  an immediate inotify event, applying layout, scaling, and theme adjustments
  in under 1 millisecond.

4.3. Instant Master Switch & Dynamic Geometry Engine
- Master ON/OFF toggle alters window visibility state in memory rather than
  killing or relaunching underlying processes, delivering zero switch delay.
- `calcHudDimensions()` dynamically recalculates line heights and bounding
  boxes to prevent metric clipping across screen rotations and aspect ratios.

4.4. Telemetry Metrics, Presets & Color Themes
- Metric Toggles: FPS, Frametime, CPU load/freq, CPU scaling governor, Mali GPU
  load/freq, Mali GPU governor, RAM, ZRAM swap, Battery wattage/temp, and Network.
- One-Tap Presets:
  * Minimal : Pure FPS counter for competitive esports.
  * Compact : Balanced essentials (FPS, CPU, GPU, Battery).
  * Detailed: Full telemetry stack including active scaling governors.
- Color Themes: Cyber Neon, AMOLED Dark, Matrix Green, Crimson Red, and Custom.

================================================================================
5. PERFORMANCE PROFILES SPECIFICATION & DYNAMICS
================================================================================

1. SLEEP (SCREEN OFF / IDLE):
   - Engaged automatically when the display turns off.
   - CPU scaling governor switched to powersave or conservative schedutil limits.
   - uclamp min reduced to 0; uclamp max clamped to suppress idle wakeups.
   - Mali GPU throttled to minimum hardware frequency (130 MHz / 260 MHz).

2. INTERACTIVE (DEFAULT / DAILY USE):
   - Active profile for daily navigation, browsing, and messaging.
   - GPU devfreq ceiling uncapped up to ~1003 MHz with responsive 65% upthreshold
     to eliminate UI frame drops.
   - Balanced uclamp min (10–20%) ensuring instant app launches.

3. GAMING (HIGH PERFORMANCE):
   - Triggered automatically when a standard 3D game launches.
   - CPU uclamp min raised to 30–50% to prevent frame drops.
   - cpufreq up_rate_limit accelerated to 1000 µs.
   - GPU floor elevated (390 MHz / 520 MHz) with `gx_game_mode=1`.

4. GAMING MOBA (ULTRA LOW-LATENCY):
   - Reserved for competitive titles (e.g., Mobile Legends, Wild Rift, PUBG).
   - Maximum safe CPU uclamp min (60–80%) for instant touch-to-frame rendering.
   - GPU floor elevated to 650 MHz / 800 MHz.
   - Helio DVFSRC QoS engaged for lowest memory bus interconnect latency.

================================================================================
6. MATERIAL DESIGN 3 WEBUI DASHBOARD (VUE 3 + PINIA SPA)
================================================================================

6.1. Telemetry Store Resilience & Zero-Flicker Architecture
- Pinia state store (`webui/src/stores/hyper.js`) handles asynchronous polling:
  * `pollCpuGpu()` : Runs every 1s for CPU/GPU loads and temperatures.
  * `pollRamBat()` : Runs every 2s for RAM, ZRAM, and battery telemetry.
  * `pollCharger()` : Runs every 2s for PMIC wattage and charging speed.
- Multi-tier fallback prevents UI flicker: if socket queries experience brief
  contention, previously cached daemon PIDs and states are retained.

6.2. Single-File Artifact Distribution & View Modules
- Bundled into a single self-contained HTML document (`webroot/index.html`) using
  `vite-plugin-singlefile`, avoiding local web server dependency.
- Main Views:
  * `HomeView.vue`    : Live hardware hero card, HyperMoon quick toggle, and status.
  * `ChargerView.vue` : 16-level hardware charging speed slider and battery safety.
  * `GamesView.vue`   : Game profile assignment, auto-detector, and app picker.
  * `HudView.vue`     : HyperMoon appearance, presets, geometry, and metric toggles.
  * `LogsView.vue`    : Streaming daemon logs, bug report generation, and terminal.
  * `AboutView.vue`   : System specs, kernel details, changelog, and developer info.

================================================================================
7. INSTALLER LIFECYCLE & SECURITY VALIDATION
================================================================================

7.1. Module Installation Script (customize.sh)
- Kernel Compatibility Verification: Asserts Linux Kernel 5.10–6.12+ GKI.
- Hardware Validation: Verifies MediaTek MT6789 Family nodes (GED & Mali GPU).
- Daemon Upgrade Guard: Terminates running instances and purges old sockets
  before extracting new archives.
- User Data Preservation: Merges existing user `gamelist.txt` and hardware
  charging configurations from `/data/adb/hypercore/`.
- Automatic Game Detection: Scans installed non-system packages (`pm list packages -3`)
  and registers detected games automatically.
- Integrity Check: Executes `libhypercore.so --verify-integrity` against `checksums.txt`.

7.2. Boot Service Launcher (service.sh)
- Awaits complete system initialization (`sys.boot_completed == 1`).
- Universal ROM Shield: Scans and neutralizes conflicting thermal or tweaking
  scripts in `service.d` and `post-fs-data.d`.
- Launches `libhypercore.so` with automatic failure retry.
- Launches HyperMoon daemon and overlay processes if configured active.

7.3. Post-FS-Data Initialization (post-fs-data.sh) & Uninstaller (uninstall.sh)
- `post-fs-data.sh`: Directly writes early boot virtual memory settings.
- `uninstall.sh`: Completely terminates background processes, restores CPU
  governors and frequencies, resets Mali power policies, cleans up PATH symlinks,
  and purges module runtime directories.

================================================================================
8. BUILD & DEPLOYMENT AUTOMATION (build.sh)
================================================================================
The repository build system (`build.sh`) manages the full build lifecycle:
1. Compiles WebUI into single-file `webroot/index.html` via Vite.
2. Updates `checksums.txt` and generates `src/include/embedded_checksums.hpp`.
3. Compiles C sources with Termux/NDK Clang using `-O3 -flto -s` flags.
4. Packages clean release zip into `/data/data/com.termux/files/home/HyperCore_Releases/HyperCore-vX.Y.Z-Unified.zip` (`~/HyperCore_Releases/`).
5. When `--deploy` is specified, updates `/data/adb/modules/hypercore/` in-place
   and restarts active daemon instances.

================================================================================
9. DEVELOPMENT INVARIANTS & SYSTEM RULES
================================================================================
1. Zero Frequency Locking: Frequencies must remain dynamic under governor logic.
2. Target Hardware Scope: Strictly MediaTek MT6789 Family (Helio G99/G100/G200).
3. GKI Sysfs Fallback: Always use fallback arrays for Cgroup and kernel nodes.
4. Zero Storage Pollution: Build release ZIPs exclusively in `~/HyperCore_Releases/` (`/data/data/com.termux/files/home/HyperCore_Releases/`).
5. Git Branch Workflow: Active development on `experimental`, fast-forward merged to `main`.
6. Language Standard: 100% natural, clean English across all code, UI, and documentation.
================================================================================
